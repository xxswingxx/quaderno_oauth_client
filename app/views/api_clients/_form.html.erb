<%= form_for(@api_client) do |f| %>
  <% if @api_client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_client.errors.count, "error") %> prohibited this api_client from being saved:</h2>

      <ul>
      <% @api_client.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :token %><br />
    <%= f.text_area :token %>
  </div>
  <div class="field">
    <%= f.label :refresh_token %><br />
    <%= f.text_area :refresh_token %>
  </div>
  <div class="field">
    <%= f.label :token_expires_at %><br />
    <%= f.text_field :token_expires_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
